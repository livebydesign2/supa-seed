# Supa Seed Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================
# REQUIRED CONFIGURATION
# =============================================

# Your Supabase project URL
# Find this in your Supabase Dashboard > Settings > API
SUPABASE_URL=http://127.0.0.1:54321

# Your Supabase service role key (NOT anon key!)
# Find this in your Supabase Dashboard > Settings > API
# This key has admin privileges and should be kept secure
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================
# OPTIONAL CONFIGURATION
# =============================================

# Environment setting (affects logging and behavior)
# Options: local | staging | production
NODE_ENV=local

# Unsplash API key for real images (optional)
# Get your API key from: https://unsplash.com/developers
# If not provided, placeholder images will be used
UNSPLASH_ACCESS_KEY=your-unsplash-access-key-here

# =============================================
# DEVELOPMENT SETTINGS
# =============================================

# Set to true to enable debug logging
DEBUG=false

# Custom database connection (if not using SUPABASE_URL)
# DATABASE_URL=postgresql://user:password@localhost:5432/database

# =============================================
# TESTING CONFIGURATION  
# =============================================

# Test environment variables (used by test suite)
# These can be the same as your development values
TEST_SUPABASE_URL=http://127.0.0.1:54321
TEST_SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# =============================================
# SECURITY NOTES
# =============================================

# IMPORTANT SECURITY CONSIDERATIONS:
# 
# 1. NEVER commit your actual .env file to version control
# 2. The service role key has admin privileges - keep it secure
# 3. Use different keys for development, staging, and production
# 4. Rotate your keys periodically for better security
# 5. In production, use environment variables instead of .env files
#
# For production deployments, set these environment variables directly
# in your hosting platform (Vercel, Railway, Docker, etc.)